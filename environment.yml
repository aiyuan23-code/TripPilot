name: trippilot

channels:
  - conda-forge

dependencies:
  - python=3.11
  - pip
  - pip:
      - pydantic>=2.10,<3
      - pydantic-settings>=2,<3
      - python-dotenv>=1,<2
      - langgraph>=1.2,<2
      - langchain-openai>=1.1,<2
      - langchain-mcp-adapters>=0.3,<1
      - uv>=0.8,<1
      - pytest>=8,<9
